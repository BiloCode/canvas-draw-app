import styles from "./styles.module.scss";

const NavigationBar = () => {
  return (
    <nav className={styles.navigation}>
      <h1>Draw Canvas App</h1>
    </nav>
  );
};

export default NavigationBar;
